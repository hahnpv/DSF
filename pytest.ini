[pytest]
testpaths = python/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    qt: tests that require a running QApplication (pytest-qt)
    slow: tests that run the full C++ simulation loop
